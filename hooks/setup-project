#!/usr/bin/bash
set -e

VERSION=$(cat "$SDK/VERSION")
VENV_DIR="$HOME/.tau/venv"

if [ ! -d "$VENV_DIR/bin" ]; then
    python3 -m venv "$VENV_DIR"
    "$VENV_DIR/bin/pip" install --quiet --upgrade pip
fi

"$VENV_DIR/bin/pip" install --quiet "tau-ai==${VERSION}"
